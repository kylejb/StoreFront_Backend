class Payment < ApplicationRecord
    
    def build_payment_object(current_order)
        #TODO - construct Payment Object from Client-side request and pass object through Stripe_Api model
    end
end